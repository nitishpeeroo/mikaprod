$(document).ready(function (e) {
    var blockArtiste = $('.artistecol');
    var nbBlock = blockArtiste.length;
    var taille = nbBlock * 200;
    var section = $('#section-artiste');
    section.height(taille);

});